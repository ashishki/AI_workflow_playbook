# Playbook: программа доведения до рабочего использования

Дата: 20 сентября 2026. База: `0c4027f8ecfc0b086c39613bb8c83a91b962d139`.
Ветка исполнения: `feature/playbook-delivery-20260920`, отдельная от master.
Это продолжение после слитого PR #9, не повторная миграция vNext.

## Результат и границы

Engineering должен помогать автору реально разрабатывать новые и текущие проекты,
а также отдельно испытывать подходы. Product должен вести владельца небольшой
рабочей проблемы к подходящему изменению и дальнейшему владению решением. Shared
остаётся единственной основой повторно используемых механизмов.

Полный путь: Discover → Decide → Design → Build → Verify → Introduce → Observe →
Change → Diagnose → Recover → Transfer → Grow → Retire. Не каждый запрос обязан
проходить все стадии. Никакого потолка числа skills и остановки на первом каркасе.
Результатом может быть процесс, настройка, таблица, скрипт или программа без AI.

По поручению владельца исходники и проверки готовятся сейчас. Полные тесты,
браузер, реальные model runs и независимые агенты запускаются владельцем/агентом
на VPS позже. Эти этапы не отмечаются пройденными заранее. Доступы, расходы,
участники, публикация и изменения production остаются отдельными полномочиями.

## Единая очередь

Это единственная очередь новой программы. Старые M0–M4/CF/PA-очереди не являются
заданиями этой ветки. PA относится к другому репозиторию. Статус `исходники готовы`
означает только реализацию и подготовленные тесты; он не означает приёмку.

| Шаг | Зависит от | Конкретная работа | Критерий приёмки | Источник результата |
|---|---|---|---|---|
| D00. Достоверная основа | — | Исправить установку Harness Lab, импорт pytest, устаревший handoff; сохранить исторические результаты | Чистое окружение собирает весь suite; общий CI/verifier проходят либо каждый оставшийся дефект открыт и блокирует готовность | VPS A; CI точного коммита |
| D01. Проверка среды | D00 | Общий read-only preflight для Engineering/Product; отличать наличие CLI от авторизации, браузера и разрешённого запуска | Отсутствие зависимости видно; проверка ничего не устанавливает, не читает credentials и не запускает inference; фактические инструменты проверены отдельно | Новые unit tests; VPS B |
| D02. Рабочий Engineering | D01 | Одна точка входа для нового проекта, текущего проекта и эксперимента; применять существующий initializer/Feature Workflow/Role Runner | Три настоящих прохода, без повторного bootstrap текущего проекта, потери пользовательских изменений и ручного управления каждым review | VPS C / ENG-NEW, ENG-CHANGE, ENG-EXP |
| D03. Связный Product | D01 | Понятный разговор о процессе, выбор решения, дизайн и построение, видимый результат; не превращать Discover в анкету | NO-CODE и APP истории пройдены; настоящий результат открыт; правила не выдуманы, no-build допустим, объяснены ограничения | VPS C; наблюдение владельца |
| D04. Владение и достоверные ссылки | D02, D03 | Проверить ссылки на свидетельства, перевод статусов в человеческий язык, fresh-session Change/Transfer | Пропавший/изменённый файл и неподтверждённая ссылка не превращаются в актуальную проверку; новый агент продолжает без старого чата | Unit/CLI/archive tests; TRANSFER |
| D05. Диагностика и восстановление | D04 | Для поддерживаемого решения: схема сбоя, безопасная копия, сохранность, сверка неизвестного исхода внешнего действия | Восстановлены данные и сценарий; не выполнена повторная отправка при неизвестном исходе; отсутствие backup — blocker, не fake success | RECOVER на одноразовой копии |
| D06. Рост, внедрение, отключение | D03–D05 | Подготовить introduction/fallback; проверить роли и пределы роста, экспорт и прекращение jobs/подписок | Двое пользователей не получают чужие данные; экспорт читается; отключение проверено; реальные действия только после разрешения | GROW/RETIRE и владелец |
| D07. AI и интеграции | D03–D05 | Реальный разрешённый AI/data/tool путь: retrieval/answer/no-answer/permissions, оценка источников меток | Рабочие observations, свежие случаи, запрет утечек, корректность цитат и неопределённости; fixture не выдаётся за API | AI-EVAL; внешние проверки |
| D08. Полная агентная проверка | D00–D07 | Независимые review/fix/recheck через существующий runner/exec; честный plain-контроль и учёт полной стоимости | Проверен итоговый diff, нет открытых подтверждённых blockers; актуальное evidence; отрицательные результаты сохранены | VPS D/E; отчёт reviewer и Lab |
| D09. Реальное использование | Готовая безопасная ветка D03–D07 | Преподаватель: одна наблюдаемая проблема, использование, повторное изменение; далее 3–5 человек | Повторный визит/изменение и передача без автора; учтена вся помощь и стоимость, эффект не выдуман | product/pilots; реальные даты |
| D10. Поставка и решение о выпуске | D08, D09 | Версия/состав/совместимость, обновление/удаление, справка, права, сохранённые доказательства | Проверен фактический ZIP и lifecycle установки; владелец отдельно решил о выпуске | VPS F; решение владельца |

## Текущий срез реализации

D00/D01/D03/D04 реализуются в этой ветке как исходники, процедуры и тесты. D02
получает единый рабочий маршрут. D05–D07 — имеющиеся процедуры плюс конкретная
матрица исполнения, но реальные интеграции/restore ещё не приняты. D08–D10 ждут
VPS и настоящих участников. Вся новая runtime/model/browser приёмка — `pending`.

Это не урезание конечной цели: прекращать программу на source-ready нельзя.
После каждого VPS-этапа обновлять этот раздел ссылкой на версию, область, результаты,
незакрытое и следующий dependency-ready шаг. Не хранить второй расходящийся реестр.

## Как выполняется шаг

1. Зафиксировать исходный commit, фактическое дерево, правила проекта, критерии и риск.
2. Прочитать только контекст текущей задачи. При обнаруженном блокере сохранить его,
   а независимые разрешённые работы продолжать. Не выходить за предоставленный scope.
3. Подготовить реализацию и регрессионные проверки, включая отрицательные случаи.
   Ожидаемый RED не подменять синтаксической ошибкой или отсутствующей зависимостью.
4. На VPS выполнить релевантные проверки и обязательный общий baseline. Предыдущий
   PASS относится к прежней версии. Не ослаблять tests или frozen evidence.
5. Независимый read-only reviewer изучает точный diff/дерево. Исполнитель проверяет
   замечания, исправляет подтверждённое; affected tests и review повторяются.
6. Зафиксировать связный commit и push. Findings/открытые gates/команды/версии
   остаются доступны следующей сессии. Чистота дерева не означает release approval.

Глубокие границы: после D00–D02 (Engineering/shared), после D03–D07
(пользовательский lifecycle/data/actions), после D08 (итоговое состояние).
Новый риск доступа, внешних действий или потери данных проверяется немедленно,
а не ждёт общего этапа. Нельзя по умолчанию запускать четыре reviewer на правку текста.

## Проверяющие и модели

Основная сессия использует настройки владельца. Reviewer — свежий read-only
процесс через существующий Role Runner; остальные предусмотренные роли — exec
по действующему протоколу. Reviewer не исправляет исходники и не запускает reviewer.
Механические проверки выполняет код. Для каждой модельной задачи выбирать
быстрый достаточно надёжный вариант; сложные архитектурные/безопасностные и
спорные выводы эскалировать сильной модели. Фиксировать requested/observed model,
effort, версии, фактический scope, стоимость; неизвестное не угадывать.
Нельзя автоматически пересылать частные данные новому провайдеру при отказе старого.

`validated` runner означает валидность его execution evidence, не PASS кода.
PASS reviewer не заменяет тесты, бизнес-эффект, полномочия или решение о выпуске.
P0/P1 закрываются только исправлением и перепроверкой либо отдельным допустимым
решением владельца по политике проекта; модель не одобряет собственное исключение.

## Критерии завершения всей программы

**Engineering-ready:** настоящие новые/текущие проекты и изолированный эксперимент
проходят рабочий маршрут; контекст актуален; автор не руководит каждым механизмом.

**Product-ready в обозначенной области:** человек начинает с проблемы, получает
подходящее решение, использует, изменяет, восстанавливает и передаёт с понятными
ограничениями. Серьёзные границы своевременно требуют специалиста, не ложной DIY-гарантии.

**Польза наблюдена:** есть результат процесса, повторное использование и затраты,
отдельно показана помощь автора. A/B без преимущества остаётся таким результатом.

**Release:** действующий владелец отдельно разрешил состав, права и распространение.
Готовность этих четырёх уровней не сводится к одной галочке CI.

## Откат и сохранность

Массового переноса существующих путей нет. Shared helpers распространяются из
одного источника; полезные Governed-контракты сохраняются. Установленные downstream
и экспериментальные модели не обновляются автоматически. Откат исходников этой
ветки — обратные коммиты; возврат исходников не восстанавливает пользовательские
данные и не отменяет внешние действия. Raw traces и частные примеры не коммитить.
